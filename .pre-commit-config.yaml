default_language_version:
    python: python3.8

repos:
    - repo: https://github.com/psf/black
      rev: 22.3.0
      hooks:
          - id: black
          # Config for black lives in pyproject.toml
    - repo: https://github.com/asottile/blacken-docs
      rev: v1.12.1
      hooks:
          - id: blacken-docs
            additional_dependencies: [black==22.3.0]
    - repo: https://github.com/pre-commit/pre-commit-hooks
      rev: v4.1.0
      hooks:
          - id: trailing-whitespace
            args: ["--markdown-linebreak-ext=md,markdown"]
          - id: end-of-file-fixer
          - id: check-yaml
          - id: check-ast
          - id: fix-byte-order-marker
          - id: check-merge-conflict
          - id: debug-statements
          - id: detect-private-key
          - id: detect-aws-credentials
            args: ["--allow-missing-credentials"]
    - repo: https://github.com/uktrade/pii-secret-check-hooks
      rev: 0.0.0.35
      hooks:
          - id: pii_secret_filename
            files: ''
            language: python
            args: [exclude]
            pass_filenames: true
            require_serial: true
          - id: pii_secret_file_content
            files: ''
            language: python
            args: [--exclude=pii-secret-exclude.txt]
            pass_filenames: true
            require_serial: true
            additional_dependencies: [click==8.0.4]
          #- id: hooks_version_check
          #  name: Checking local hooks against latest release
          #  verbose: true
          #  require_serial: true
    - repo: https://github.com/uktrade/github-standards
      rev: main # This should be the latest github released tag, see https://github.com/uktrade/github-standards/releases for the latest tag
      hooks:
        - id: validate-security-scan
          verbose: true # This is helpful for debugging initial setup, it can be removed when the hooks are verified as working in your repository
          args: [-v] # This is helpful for debugging the hook logic, the output is shown in the git output
        - id: run-security-scan
          verbose: true
          args: [--verbose]
